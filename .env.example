# Copy this file to `.env` and fill in your OpenAI-compatible endpoint values.
# `.env` is gitignored — never commit it.
LLM_BASE_URL=http://localhost:8000/v1
LLM_API_KEY=your_api_key_here
LLM_MODEL=your-model-name
